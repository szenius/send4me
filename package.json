{
  "name": "bless-admin-bot",
  "version": "1.0.0",
  "dependencies": {
    "bluebird": "^3.5.5",
    "dotenv": "^8.0.0",
    "lodash": "^4.17.15",
    "telegraf": "^3.32.0",
    "telegraf-inline-menu": "^3.6.0"
  },
  "scripts": {
    "start": "npm install && node src/Bot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "8.16.0"
  }
}
